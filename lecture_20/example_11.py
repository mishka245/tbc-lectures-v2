"""
string:
character

list
+ flexible (add, remove, mutability)
- more memory, mutability

tuple
+ less memory, a bit faster, immutability
- is not flexible, immutability

dict
+ fast access, mutable
- a bit more memory,

set
+ fast access, unique elements
- can not add immutable objects(unhashable)

"""